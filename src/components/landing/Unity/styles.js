import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  unitystyle: {
    width: "100%",
    height: "calc(100vh - 70px)",
    position: "absolute",
    left: "0",
    zIndex: "0",
  },
});
